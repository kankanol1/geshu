/**
 * define all the styles used in work canvas.
 */


const getStylesForType = (str) => {
    switch(str) {
        case 'source':
        case 'preprocessor':
        case 'sink':
        case 'algorithm':
        default:
    }
}

export default {getStylesForType}